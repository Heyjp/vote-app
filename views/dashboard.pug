extends sublayout


block hero
  h3 Delete Polls
  p Click on the polls you want to remove
  
block main-content
  .row
    .col.s6
        ul.collection
          - for (var i = 0; i < data.length; ++i) {
          li.collection-item
            a(class="delete-poll" href='/poll/delete/'+data[i])= title[i]
          - }
